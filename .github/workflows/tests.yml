name: Tests
on: [push, pull_request]

jobs:
  tests:
    name: Tests
    runs-on: ubuntu-latest
    env:
      NINJARMM_CLIENT_ID: '${{ env.NINJARMM_CLIENT_ID }}'
      NINJARMM_CLIENT_SECRET: '${{ env.NINJARMM_CLIENT_SECRET }}'
      NINJARMM_ENCRYPTION_PASSPHRASE: '${{ env.NINJARMM_ENCRYPTION_PASSPHRASE }}'
      NINJARMM_BASE_URL: '${{ env.NINJARMM_BASE_URL }}'
      NINJARMM_TEST_DATA: '${{ env.NINJARMM_TEST_DATA }}'
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          go-version: '>=1.19.3'
      - run: go test
